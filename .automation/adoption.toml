version = 1

preserve_existing = [
  "README.md",
  ".envrc",
  "flake.nix",
  "flake.lock",
]

[line_merge]
".gitignore" = ["/.worktrees/", "/result", "/result-*"]

[structured_merge]
Justfile = "agent-module-router"
"AGENTS.md" = "agent-rules-block"
